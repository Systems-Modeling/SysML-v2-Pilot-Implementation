//This test is failing.  The errors generated by the original junit test are errors the ones NOT start with "The feature"" or "The required feature" error messages
//* 
XPECT_SETUP org.omg.kerml.xpect.tests.testsuite.KerMLXtextTest
	ResourceSet {
		ThisFile {}
		File {from ="/library/Base.kerml"}
	}
	Workspace {
		JavaProject {
			SrcFolder {
				ThisFile {}
				File {from ="/library/Base.kerml"}
			}
		}
	}
END_SETUP 
*/

package test{
	class non{}
	class A{
		// XPECT errors ---> "Couldn't resolve reference to Feature 'non'." at "non"
		feature aa is non;
		feature a: A;
	}
	class B{
		//The order of error and the messages(filename) are different between Junit and Junit Plugin-in Test.
		//* XPECT errors ---
		"Couldn't resolve reference to Type 'test'." at "test"
		"Relationships must have at least two related elements" at "A::"
		"no viable alternative at input '..'" at ".."
		"no viable alternative at input 'A'" at "A"
		--- */
		feature b: test..A::a;
	}
}

