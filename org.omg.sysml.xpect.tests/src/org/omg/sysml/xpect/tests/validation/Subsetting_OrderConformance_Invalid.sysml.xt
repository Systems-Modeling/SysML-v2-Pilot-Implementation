//* 
XPECT_SETUP org.omg.sysml.xpect.tests.validation.SysMLValidationTests
	ResourceSet {
		ThisFile {}
		File {from ="/library.kernel/Base.kerml"}
		File {from ="/library.kernel/BaseFunctions.kerml"}
		File {from ="/library.kernel/ScalarValues.kerml"}
		File {from ="/library.systems/Blocks.sysml"}

	}
	Workspace {
		JavaProject {
			SrcFolder {
				ThisFile {}
				File {from ="/library.kernel/Base.kerml"}
				File {from ="/library.kernel/BaseFunctions.kerml"}
				File {from ="/library.kernel/ScalarValues.kerml"}
				File {from ="/library.systems/Blocks.sysml"}
			}
		}
	}
END_SETUP 
*/
package '1a-Parts Tree Simplified' {
	
	part rearAxleAssembly {			
		part rearWheel: Wheel[2] ordered;
		// XPECT errors --> "Subsetting.subsettedFeature(rearWheel) is ordered then subsettingFeature(rearWheel_1) shall be ordered."  at "rearWheel"
		part rearWheel_1: Wheel[2] subsets rearWheel;
		// XPECT errors --> "Subsetting.subsettedFeature(rearWheel) is ordered then subsettingFeature(rearWheel_2) shall be ordered."  at "rearWheel"
		part rearWheel_2 subsets rearWheel;
		// XPECT errors --> "Subsetting.subsettedFeature(rearWheel) is ordered then subsettingFeature(rearWheel_3) shall be ordered."  at "rearWheel"
		part rearWheel_3 subsets rearWheel = rearWheel[1];
		part rearWheels: Wheel[*] ordered;
		// XPECT errors --> "Subsetting.subsettedFeature(rearWheels) is ordered then subsettingFeature(rearWheels_1) shall be ordered."  at "rearWheels"
		part rearWheels_1: Wheel[*] subsets rearWheels;		
	}
	block Wheel;
}

